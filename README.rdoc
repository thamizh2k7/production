==Sociorent Street  Readme


=Deployement Pre Reqiruements
1. Manually install 'wkhtmltopdf-binary' gem on server:
       gem install 'wkhtmltopdf-binary'

2. Check Facebook APP id set to production:
       locatied in config/initalizers/a_omniauth.rb

3. Change the PrivatePub server address:
       Located in config/private_pub.yml
