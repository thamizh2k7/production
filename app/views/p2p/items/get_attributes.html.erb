

	<tr id="new_spec_holder"></tr>

		<%@attr.each do |attr| %>
				<tr class="cat_spec">
					<td > <%= attr.name  %></td>
					<td > 
						<span class="specification canEdit"  specname="<%= attr.name %>" specid="<%= attr.id.to_s %>" id="item_<%= attr.id %>" >Enter a <%= attr.name  %>
					</span>

					</td>
				</tr>

		<% end %>
