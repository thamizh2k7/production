$(".close").trigger("click");

        if (oInboxTable){
            oInboxTable.fnDraw();
        }

        if (oSentBoxTable){
            oSentBoxTable.fnDraw();
        }
          if (oDeleteBoxTable){
            oDeleteBoxTable.fnDraw();
        }

showNotifications('Message sent');
