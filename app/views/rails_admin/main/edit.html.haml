= rails_admin_form_for @object, :url => edit_path(@abstract_model, @object.id), :as => @abstract_model.param_key, :html => { :method => "put", :multipart => true, :class => "form-horizontal denser", :data => { :title => @page_name } } do |form|
  = form.generate :action => :update
:javascript 
  jQuery(function() { 
    jQuery('#book_description').tinymce({ 
      theme: 'advanced' 
    }); 
    jQuery('#book_strengths').tinymce({ 
      theme: 'advanced' 
    }); 
    jQuery('#book_weaknesses').tinymce({ 
      theme: 'advanced' 
    }); 
    jQuery('#general_welcome_mail_content').tinymce({ 
      theme: 'advanced' 
    }); 
    jQuery('#static_page_page_content').tinymce({ 
      theme: 'advanced' 
    }); 
        jQuery('#bank_details').tinymce({ 
      theme: 'advanced' 
    }); 
  }); 